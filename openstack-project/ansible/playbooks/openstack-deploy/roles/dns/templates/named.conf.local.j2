zone "cloud.group{{ group }}" IN {
	
	type master;

	file "/etc/bind/forward.cloud.group{{ group }}.db";

	allow-update { none; };

};

zone "0.0.20.in-addr.arpa" IN {

	type master;

	file "/etc/bind/reverse.cloud.group{{ group }}.db";

	allow-update { none; };

};
