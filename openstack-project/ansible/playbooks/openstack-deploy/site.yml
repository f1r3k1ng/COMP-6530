---

- name: Deploy an openstack instance
  hosts: all
  remote_user: ubuntu
  become: yes

  vars_files:
    - "vars/vars.yml"

  roles:
    - machine_setup
    - update
    - veth
    - docker

...
